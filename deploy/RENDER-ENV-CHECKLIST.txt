# Copy values into Render → each service → Environment.
# Fill Neon / Upstash / R2 first. Same token on BOTH services for WORKER_CALLBACK_TOKEN.
# After frontend deploys, set FRONTEND_BASE_URL on the worker (no trailing slash).

--- fayda-frontend (Next.js) ---
DATABASE_URL=
REDIS_URL=
S3_ENDPOINT=
S3_REGION=auto
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET_INPUT=fayda-input
S3_BUCKET_OUTPUT=fayda-output
S3_FORCE_PATH_STYLE=true
WORKER_CALLBACK_TOKEN=
ENFORCE_FREE_TRIAL_LIMIT=false
NODE_VERSION=20.18.0

--- fayda-worker (Docker) ---
DATABASE_URL=          (same as frontend)
REDIS_URL=             (same as frontend)
FRONTEND_BASE_URL=     https://YOUR-FRONTEND-NAME.onrender.com
WORKER_CALLBACK_TOKEN= (same as frontend)
S3_ENDPOINT=
S3_REGION=auto
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET_INPUT=fayda-input
S3_BUCKET_OUTPUT=fayda-output
S3_FORCE_PATH_STYLE=true
APP_ENV=production
